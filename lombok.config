# tells Lombok that this is the root directory and that it shouldn't search parent directories for more configuration files
config.stopBubbling = true

# allows @NonNull/@Nullable annotations on fields to be copied into Lombok-generated getters/setters
lombok.copyableAnnotations += org.jspecify.annotations.Nullable
lombok.copyableAnnotations += org.jspecify.annotations.NonNull
